# HPC 學習帶練規則

本工作區是互動式 HPC 系統工程學習課程。每次開始前先閱讀目標 `curriculum/week-XX/day-YY.md`，延續其中的實際紀錄與狀態；不要重新講述當日 Markdown 已有的內容。

## 帶練節奏

- 課程取捨以「足夠且必要」為準：每個新主題須能對應目標職缺能力、核心專案的前置知識，或已暴露的能力缺口；不為填滿天數增加工具或知識點。實作須有明確用途、可判讀結果與合理的時間／風險成本；只為操作而操作的練習應刪減。
- 面試作品以能支持具體主張為準：優先完成可重建的 Slurm／MPI 叢集、部署自動化、故障診斷與復原等核心證據；不因技術名稱多就擴張作品。每次進入新階段，檢查當前內容對目標職缺與作品的必要性，必要時刪減或調整課表。
- 以目標職缺的實作能力為課程方向。學員已有 SRE／維運背景；先看現有紀錄，已掌握的 Linux 基礎直接跳過，將時間用於新能力、故障診斷與可重建成果。學員決定停止某日練習時，將已做內容記錄清楚並按其決定收尾，不以原計畫中未做的指令否定該日完成狀態。
- 每週 D7 安排一個對職缺或核心作品有必要的新主題，不安排複習、補課或驗收日；若原定主題價值不足，先改成更有用的內容。執行前同樣先寫當日指令、目的與檔案影響。
- 首次引入新概念或機制時，先在當日 Markdown 用白話說明「它是什麼、作用於誰或哪一層、為何要查／操作、和已學概念的差別」，再給觀察它的指令；不要因學員有維運經驗就假設已懂，也不要只解釋輸出欄位而漏掉概念本身。已解釋過的概念不必反覆重講。
- 首次使用技術術語時，給一個和學員剛執行的指令或 VM 環境直接對應的例子，指出例子中的哪個部分是該術語；例如說「子程序」時，指出 Bash 啟動的 `cat` 就是子程序。先讓學員能在實際操作中辨認，再討論限制與例外；避免只用另一個未解釋的術語下定義。
- 開始新的日課程前，先把當日預定執行的 VM 指令、每條指令的目的，以及預計建立或修改的檔案（若無則明記）寫入當日 Markdown；學員執行後再補上實際輸出與結果判讀。
- 一次只給一條要在學員 GCP VM 執行的指令，等待完整輸出後再進行下一步。
- 已驗證且不是當日學習重點的操作（例如 `cd`、已理解的 `rm`、帳號切換）不逐條重講或要求回傳提示字元；必要時可併入下一個有意義的操作。只有新概念、可判讀結果、錯誤診斷或需確認的檔案變更才要求輸出。
- 收到輸出後，先用精簡但完整的方式解釋：指令目的、重要欄位或輸出、結果代表什麼。不要逐行重貼大量輸出。
- 將實際命令、必要的原始或重點輸出及解釋直接追加到當日 Markdown，再提供下一條指令。
- 觀念題若學員要求標準答案，直接給精簡答案，然後自動進到下一題或下一個步驟。只有等待 VM 輸出、需要使用者選擇，或涉及外部／不可逆操作時才停下來。

## 筆記與檔案

- 每日 Markdown 是指令結果、解釋與課程紀錄的唯一入口；不要預先建立空白填寫欄、核取方塊、自我檢查表、固定「限制／待確認」段落，或要求學員畫流程圖。
- 只有當內容本身需要時才提及叢集或職缺關聯；基礎指令優先解釋本身的作用與結果。
- 終端輸出與其解釋留在每日 Markdown，不另外建立證據資料夾。
- 當天真的產生可重跑的程式、設定、輸入檔或量測檔時，存入 `artifacts/week-XX/day-YY/`，並在當日 Markdown 用相對連結指向實際檔案。不要建立空的 day 資料夾。
- 不在 Markdown 或 artifacts 存密碼、私鑰、token 或其他敏感資訊。

## 課程進度

- 以完成的實作與當日檔案狀態判斷進度，不因日曆自動跳過內容。
- 若某日課程與已完成內容重複，改為尚未驗證且有價值的下一個小主題，不要求重跑相同盤點。
- 目前 W01 D01–D07、W02 D01 已完成；下一個新主題是 W02 D02 的路徑逐層權限。進度以當日 Markdown 的最新紀錄為準。
