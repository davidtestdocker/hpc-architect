# AlmaLinux 10.2 單節點練習：同一台 VM 同時擔任控制節點與運算節點。
# 叢集名稱僅供這個練習識別。
ClusterName=hpc_lab
# 控制節點是目前這台 VM；括號內是其私有 IP，不可直接套用到別台 VM。
SlurmctldHost=instance-20260923-104239(10.140.0.2)
# 控制服務以 slurm 帳號執行；節點與工作憑證透過 MUNGE 驗證。
SlurmUser=slurm
AuthType=auth/munge
CredType=cred/munge

# 控制端保存排程狀態；運算端暫存工作執行所需檔案。
# 目錄須分別供 slurm 帳號與 root 寫入，建立目錄是後續步驟。
StateSaveLocation=/var/spool/slurmctld
SlurmdSpoolDir=/var/spool/slurmd

# 以 CPU 和記憶體作為排程資源；這不等於已啟用核心層的記憶體隔離。
SelectType=select/cons_tres
SelectTypeParameters=CR_CPU_Memory

# 指定本機為運算節點：2 個邏輯 CPU，宣告 3000 MiB 可供排程，
# 其餘記憶體留給作業系統與控制服務；UNKNOWN 等運算服務註冊後更新。
NodeName=instance-20260923-104239 NodeAddr=10.140.0.2 CPUs=2 Sockets=1 CoresPerSocket=1 ThreadsPerCore=2 RealMemory=3000 State=UNKNOWN
# 將上述運算節點放進預設 debug 分區，每個工作最長執行 30 分鐘。
PartitionName=debug Nodes=instance-20260923-104239 Default=YES MaxTime=00:30:00 State=UP
